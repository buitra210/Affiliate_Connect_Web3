import { Box, Typography } from "@mui/material";

export default function ComingSoon() {
  return (
    <Box
      sx={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "300px" }}
    >
      <Typography variant="h3">Coming Soon</Typography>
    </Box>
  );
}
